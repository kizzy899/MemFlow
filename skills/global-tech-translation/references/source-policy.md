# 来源合规策略

执行抽取与翻译前，使用以下检查清单：

1. 确认 URL 为公开可访问，不绕过鉴权机制。
2. 输出中保留来源标注（`source_url`、域名、抓取时间）。
3. 优先使用 RSS/API 或公开页面，便于重复采集。
4. 纯机器翻译内容不直接发布，必须人工复核。
5. 按域名维护平台条款与使用注意事项。

最小输出合规要求：

1. `en.md` 与 `zh.md` 必须包含原文 URL。
2. 输出包中必须保留图片来源映射关系。
3. 在编辑交接前保留 QA 结论（`ready` 或 `needs_review`）。
